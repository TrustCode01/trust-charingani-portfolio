"use client"
import { motion } from 'framer-motion'
const Resume = () => {
  return (
    <motion.section id="resume" className='w-full max-w-[90%] sm:max-w-[85%] md:max-w-[80%] lg:max-w-[75%] mx-auto px-4 sm:px-8 md:px-12 py-8 sm:py-12 md:py-16 flex flex-col gap-12 sm:gap-16'>
        resume and certifications
    </motion.section>
  )
}

export default Resume